model Algorithm {
  id       Int       @id @default(autoincrement())
  research Research?
  x        Json?
  w        Json
  y        Int
  s        Int
  neuron   Int
  epoch    Int

  @@map("algorithm")
}
