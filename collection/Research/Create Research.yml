info:
  name: Create Research
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/research"
  body:
    type: json
    data: |-
      {
        "title": "Обратное 1223",
        "type": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
