info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "skvortsov.oleg.am@gmail.com",
        "password": "12345",
        "rememberMe": "True"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
